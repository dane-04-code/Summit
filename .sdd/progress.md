Task: Scaffold — complete (commit 9426f61, branch build/first-pages, test+tsc green)
Task: Connect screen — complete (commit 3b6e36f, design-system faithful, tsc+test green)
Task: Agent screen — complete (commit d55b9bf, hybrid layout, FlashList, faithful, tsc+test green)
Task: Agent connection + persistence backend — complete (docs/AGENTS.md + src/agents/ + src/db/; SecureStore secrets, expo-sqlite store, adapter abstraction (Hermes real / OpenClaw stub), AgentProvider rehydrates on boot; tsc+lint+test green, 28 tests). Not yet wired into chat screen (stub stream remains).
Task: Wiring core loop — complete (direct-mode connect helper + tests; Connect screen + no-agent route guard; stream reducer + tests; chat screen wired to live Hermes adapter + SQLite persistence; tsc+test green). App now works end-to-end against a real Hermes server.
Task: Relay walking skeleton (slice 3a) — complete (commits 8b195d2→670c046; monorepo layout /relay/ /connector/ /protocol/; Cloudflare Worker + PairingChannel Durable Object relay; Go connector (written, not yet deployed); RelayClient + RelayAdapter + pair screen as primary onboarding; tsc+test green: 79 app tests + 9 relay logic tests). Relay deploy to Cloudflare + connector install script are slice 3c.
Task: Pair-screen error states (Pass 1 / Frontend Pages #1) — complete (RelayClient now throws typed `RelayError`s from `src/agents/relay/errors.ts` — `relay_unreachable`, `code_not_found`, `code_expired`, `already_paired`, `agent_disconnected` — each with user-readable copy instead of bare "WebSocket error"/raw protocol reason; pair.tsx surfaces the message and only reddens the code field for code-specific failures via `isPairingCodeError`; tsc + test green, 104 passing incl. new errors + client classification tests). Remaining Pass 1: manual pairing QA matrix against real Hermes/relay.
Task: Relay deploy + install script (slice 3b/3c) — complete (commits a77fa93→14e97f5; relay LIVE at relay.summitapp.dev; GitHub Actions deploy-relay.yml auto-deploys on push; wrangler@4 + new_sqlite_classes migration required for Cloudflare free plan; connector binaries built via release-connector.yml → GitHub Releases connector-latest tag for linux/darwin amd64+arm64; install script live at get.summitapp.dev/connect — detects arch, auto-reads Hermes API key, daemonizes via systemd/nohup, prints code; relay Worker also serves get.summitapp.dev on same Worker). Pair screen redesigned: 3-step layout — copyable agent prompt, copyable curl command, 6-digit code entry.
Task: Rich output — wide-table overflow (Pass 4, slice 1) — complete (added markdown fixtures at `__tests__/fixtures/markdown.ts` — wide table, nested lists, unbreakable URL, wide code, mixed doc, huge doc — and a `@testing-library/react-native` render harness `__tests__/markdownRender.test.tsx` that drives every fixture through the real `RichMarkdown` without crashing; TDD'd a `table` render rule in `src/ui/chat/richMarkdown.tsx` that wraps wide tables in a horizontal ScrollView (`testID=md-table-scroll`) with an 88px cell minWidth, so wide tables scroll instead of clipping/squishing; confirmed `CodeBlock` already scrolls long lines. tsc + test green, 111 passing. RNTL note: `render` populates `screen` async here — query via `await waitFor(() => screen.getByTestId(...))`, not sync. Remaining Pass 4: streaming partial-markdown, graph-output decision, on-device visual pass.)
Task: UI polish & flow pass — complete (spec docs/superpowers/specs/2026-07-03-ui-polish-design.md, plan docs/superpowers/plans/2026-07-03-ui-polish-pass.md; throttled stream flushes (shouldFlush, 60ms) + removal of the forced scroll-to-end so readers can scroll up mid-stream, stop-while-streaming button that settles partial replies, markdown type ramp fixed to 24/20/17 with 17px body and grey (non-accent) bullets, long-press copy on any message with a Copied toast (blocksToText/messageToText), sidebar long-press rename/delete chats (sessionActions.renameSession), shared ScreenHeader adopted by cron list/detail + settings shell + pair, success/scrim/control theme tokens + raw-hex cleanup + robot mark → Mountain, cron summary dot only accent while a job runs, pair screen restructured to an honest two-step flow with KeyboardAvoidingView and no autoFocus keyboard slam; tsc + test green, 122 passing; new code lint-clean — 11 pre-existing lint errors remain in untouched files.)

Task: Run-approval app-side plumbing — complete (TDD). Fixed the three defects that made approval a façade: (1) added `{ type: 'approval'; runId; title; command }` to `StreamEvent` (src/agents/adapters/types.ts) + a `pendingApproval` field on `LiveTurn` that `reduceTurn` sets (keeps the turn open — an approval gate isn't `done`); (2) threaded `runId` onto the `role: 'action'` message (`Message = … & ApprovalRequest` in src/ui/chat/types.ts) so the decision can reach the run; (3) new src/ui/chat/approval.ts — `buildApprovalMessage` / `submitApproval` / `submitStop` helpers, unit-tested. NOTE: the index.tsx `handleApprove`/`handleStop` rewiring to `adapter.approveRun`/`stopRun` was stripped as out-of-scope bleed during the slash-commands Task 3 review (see .superpowers/sdd/progress.md) — the screen handlers still only swap local text, so wiring index.tsx to approval.ts is still TODO. seed.ts + blocksToText test updated for the required `runId`. tsc + test green, 196 passing (new: streamReducer approval cases + approval.ts unit tests); new code lint-clean (3 pre-existing index.tsx lint errors at lines 198/214 untouched). REMAINING (real-Hermes lightup, tracked follow-up): the chat still sends via `POST /v1/chat/completions`, which per FRAMEWORKS.md does NOT carry approvals — approvals live on the Runs API (`POST /v1/runs` → `GET /v1/runs/{id}/events` SSE → `/v1/runs/{id}/approval`). To make a real Hermes actually emit a card, the send path must move onto the Runs API and the exact approval-pending event JSON must be confirmed (not documented in repo/mock/FRAMEWORKS.md; mock-agent.mjs only accepts the resolution call, never emits the pending event). The plumbing above is verified end-to-end at the app layer against a synthetic approval event.

Task: Onboarding & auth pages (2026-07-05) — complete (spec docs/superpowers/specs/2026-07-05-onboarding-auth-pages-design.md, plan docs/superpowers/plans/2026-07-05-onboarding-auth-pages.md; commits ab4fe9c→8e7972d). New Welcome landing at src/app/(auth)/welcome.tsx — logo-glow backdrop behind a mountain-peak PeakGlyph, Summit wordmark + tagline, swipeable 4-card "what's included" pager with accent dots, ink-fill Get started CTA (→ sign-up, or sign-in when SIGNUP_ENABLED is off) and sign-in link; gentle core-RN Animated fade-up (Reanimated avoided — no jest mock). RouteGuard now lands signed-out users on /(auth)/welcome. Sign-in rebuilt to mirror sign-up's structure (BrandMark, labeled inputs w/ focus states, eye toggle, Continue with Apple, gated footer link) — Supabase calls unchanged, email now trimmed. Shared src/ui/BrandMark.tsx (PeakGlyph + BrandMark tile) and src/ui/authIcons.tsx (EyeIcon, AppleLogo); sign-up switched to them (old person-glyph mark deleted) + subtitle copy tightened. Pair screen untouched (already the post-auth landing via AgentGuard). tsc + test green, 205 passing (new: brandMark, welcome, signIn suites). RNTL note: fireEvent.changeText also needs `await act(...)` wrapping here, not just render.
